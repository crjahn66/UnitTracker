- Dashboard detail list now shows 2 columns (North | South) with compact rows
- New 4-segment per-stage bar replaces the single % bar — at a glance see which stage each unit is in (green=complete, yellow=in-progress, red=stuck, gray=pending)
- Roughly 4× more units visible without scrolling
