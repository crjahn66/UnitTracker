- Fixed Ready for Master completion counts so dashboard, reports, cards, and the Excel Completed Units tab follow the latest non-deleted Ready for Master log entry.
- Recalculated Ready for Master summary fields after log edits/deletes so the stored status stays aligned with the log history.
- Replaced the dashboard grid legend's In Progress indicator with a Priority P indicator.
