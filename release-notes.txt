- Fixed expired Supabase JWT sessions by enabling token persistence and auto-refresh.
- Pushes now read and merge the latest Supabase row before writing, preventing stale local data from rolling back Ready for Master.
- Ready for Master backfill now runs before every cloud push, not only during full sync.
- Stale-push protection now includes Ready for Master status and transition history.
