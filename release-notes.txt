- Fixed Optimo Mode updates being reverted during cloud push.
- Fixed RED/ACS/N/A unit card updates being reverted during cloud push.
- Complete counters now use the manual Ready for Master status only.
- Units count as Complete when Ready for Master is marked Good.
- Complete filters, fleet totals, reports, and Completed Units export now follow that same Ready for Master rule.
- Stage progress remains separate from the final Ready for Master completion signal.
- Completed Units Log reset marker remains in place to prevent stale test log entries from re-uploading.
- Ready for Master status resets now have a reset marker so stale local Good/Bad values are dropped instead of re-uploaded.
