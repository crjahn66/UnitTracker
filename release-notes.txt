- Fixed Optimo Mode updates being reverted during cloud push.
- Fixed RED/ACS/N/A unit card updates being reverted during cloud push.
- Local edits now merge remote issue/photo additions without clobbering the field just changed.
- Completed Units Log data was reset in Supabase so old test transition entries no longer export.
