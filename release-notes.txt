- Fixed updates disappearing when stale clients pushed older local data over newer Supabase data.
- Cloud pushes now perform a full timestamp-aware merge before writing.
- Supabase writes now abort if another device changes the row between fetch and push.
- Component, misc, Ready for Master, and general issue merges now keep the newest dated edit.
