- App now sends its UnitTracker version with every Supabase request.
- This supports a Supabase-side minimum-version write block for stale clients.
- Older clients without the version header can be blocked from overwriting sync_state.
- Ready for Master remains manual; stale overwrite protection remains active.
