{
  "ignoreCommand": "node -e \"process.exit((process.env.VERCEL_GIT_COMMIT_REF || '').startsWith('android-test-') ? 0 : 1)\"",
  "installCommand": "npm install --legacy-peer-deps",
  "buildCommand": "npx expo export --platform web && node scripts/inject-error-handler.js && node scripts/inject-version.js",
  "outputDirectory": "dist",
  "framework": null
}
